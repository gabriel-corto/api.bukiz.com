generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id
  avatar    String?
  name      String?
  email     String   @unique
  createdAt DateTime
  updatedAt DateTime

  @@map("users")
}
